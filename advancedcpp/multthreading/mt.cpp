#include <iostream>
#include <pthread.h>
#include <cstdlib>


// mt -> programs run concurrently
// two types : process-based & thread based 
/** 
 *  process-based = concurrent execution of programs
 *  thread-based = concurrent execution in same programs 
 *  mt programs contain two or more parts each part is called a thread
 *  each thread has a seperate execution path
 */

// program used posix threads 











int main() {

    return 0;
}